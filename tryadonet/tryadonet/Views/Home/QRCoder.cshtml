
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>QRCoder</title>
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
    <div class="container"> 
        加了一些乱七八糟的文字描述
        <img id="qrcode" src="@Url.Action("CreateQrCode")" />
    </div>

</body>
</html>
<!--
<script src="~/scripts/jquery.min.js"></script>
<script src="~/scripts/jquery.qrcode.min.js"></script>
<script>
    jQuery(function () {
        var host = window.location.host;
        jQuery('#qrcode').qrcode(host + "/Home/Detail?id=100"); 
    })
    var uu = window.location.href; //这个方法可以获得当前网页的url
    var obj;
    obj = document.getElementById("url_present");
    obj.innerHTML = uu;

    //第二种
    var host = window.location.host;
    jQuery('#qrcodeCanvas').qrcode({
        render: "canvas",       
        text: host + "/Home/Detail?id=100",
        width: "200",               //二维码的宽度
        height: "200",              //二维码的高度
        background: "#ffffff",       //二维码的后景色
        foreground: "#000000",        //二维码的前景色
        //src: 'logo.png'             //二维码中间的图片
    });
    $('#saveQrCode').click(function () {
        var canvas = $('#qrcodeCanvas').find("canvas").get(0);
        console.log(canvas);
        try {//解决IE转base64时缓存不足，canvas转blob下载
            var blob = canvas.msToBlob();
            navigator.msSaveBlob(blob, 'qrcode.jpg');
        } catch (e) {//如果为其他浏览器，使用base64转码下载
            var url = canvas.toDataURL('image/jpeg');
            $("#download").attr('href', url).get(0).click();
        }
        return false;
    });
</script>-->